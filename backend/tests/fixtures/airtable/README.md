# Airtable recorded responses

Hand-recorded from the shapes documented in Airtable's Web API reference (list records,
create records, update record, get base schema, create table, and the error envelope).
`placeholder` values are replaced by the tests. No test ever calls Airtable.
