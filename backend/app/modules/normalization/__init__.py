"""Turning one source's record into one shape (blueprint slide 31).

Nothing here guesses. A value that cannot be parsed with confidence becomes `None`, and
`NormalizedBusiness` is the only thing entity resolution is ever allowed to see.
"""
