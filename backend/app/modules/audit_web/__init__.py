"""Deterministic, evidence-backed audits of a business's own homepage (v0.4.0).

Named `audit_web` so it cannot be confused with `modules/audit`, which is the audit log.
Nothing here judges a website: every output is an observation with the verbatim text it
was read from and the URL it was read at.
"""
