"""AI classification (v0.5.0): a provider-agnostic LLM client, the prompt, the strict
output schema, the guardrails that every model answer must pass, model routing and the
cost controls. Nothing in this package is trusted on its own word."""
