/** Constants the review pages share. */

/** Where the queue page keeps the order the detail page walks with j / k and "Next". */
export const QUEUE_ORDER_KEY = "radar.queue.order";

/** What the UI says on a 409 from a decision: someone else got there first. */
export const CONFLICT_MESSAGE = "Another reviewer already decided this";
